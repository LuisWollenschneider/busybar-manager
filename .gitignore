node_modules/
logs/
apps/
.venv/
__pycache__/
.DS_Store
config.json.tmp*
# Local runtime config: holds personal args (coords) and any library token —
# never commit. Ship config.example.json as the template instead.
config.json
config.json.corrupt-*
# Local docker env: host bind address and any override URLs. Ship
# .env.example as the template instead.
.env
# Local Claude Code permission overrides
.claude/settings.local.json
